# ProtonVPN Docker container .env
# Copy this file to docker-pvpn/.env and fill in the values.
# Security: Do NOT commit real credentials. Add docker-pvpn/.env to .gitignore if needed.

# ProtonVPN credentials
PVPN_USERNAME=you@example.com
PVPN_PASSWORD=your-protonvpn-password

# Connection target
# Set exactly one of the following; leave both empty to connect to the fastest server.
# Example server format: US-CA#1
PVPN_SERVER=
# Country code example: US, DE, NL
PVPN_COUNTRY=US

# Connection options
# Protocol: udp or tcp (udp recommended)
PVPN_PROTOCOL=udp
# Killswitch: on or off
PVPN_KILLSWITCH=on
# ProtonVPN DNS: on or off
PVPN_DNS=on

# Optional: container timezone for logs (if your base image honors TZ)
TZ=UTC